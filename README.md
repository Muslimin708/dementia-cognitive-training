# Refactored Dementia Cognitive Training App

Copy your existing `database.py` into this folder. Also copy the existing custom Streamlit component frontend into:

`components/face_annotation_component/`

Run:

```bash
pip install -r requirements.txt
streamlit run app.py
```

The old `<br>` and escaped HTML artifacts are not valid Python and were removed.
